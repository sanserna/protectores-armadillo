/**
 * CONTENIDOS
 *
 * FRAMEWORKS
 * normalize.css................Estandariza los estilos.
 * animate.css..................Colección de animaciones.
 * jquery.fullPage.css..........Transiciones entre las secciones de la pagina.
 *
 * BASE
 * _fonts.......................Configuracion de la fuente principal.
 * _base........................Estilos base en los elementos de la pagina.
 * _base_mixins.................Algunos mixins utiles.
 * _base_variables..............Declaracion de variables globales.
 *
 * COMPONENTES
 * .content.....................Contenedor principal.
 * #video-container.............Contenedor del video del landing page y sus elementos.
 *
 * COMPONENTES
 *
 */

// frameworks imports
@import 'frameworks/normalize'
@import 'frameworks/animate.css'
@import 'frameworks/jquery.fullPage.css'

// Base imports
@import 'base/_fonts'
@import 'base/_base'
@import 'base/_base_mixins'
@import 'base/_base_variables'

// Tus estilos van aqui
#video-container
    background-color black
    min-height 100vh
    overflow hidden
    position relative
    max-height 100vh
    width 100%

#home-video
    display block
    height 100%
    width 100%
    position absolute
    top 0
    left 0
    object-fit cover
    z-index 0

#logo-bg
    position absolute
    top 0
    left 0
    height 100%
    width 100%
    background rgba(black,.3) url(../img/pattern.png)
    z-index 0

#lr-logo
    area()
    position relative
    width 20%
    //left 50%
    //margin-left -35%
    //top 75px
    z-index 1

// .logo-bg
    // background rgba(0,0,0,.5) url(../img/pattern.png)

// #home-video
    // display none




















